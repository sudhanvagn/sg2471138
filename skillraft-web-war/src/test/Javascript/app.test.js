define(['app'], function (app) {
    'use strict';

    describe('app', function () {
        it('should exist', function() {
            should.exist(app);
        });
    });
    
    describe('Main', function () {
        it('should exist', function() {
            should.exist(main);
        });
    });
});